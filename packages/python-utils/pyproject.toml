[project]
name = "python-utils"
version = "0.1.0"
description = "Python utilities for the project"
readme = "README.md"
authors = [
    { name = "fhajari", email = "fhajari@ncai.gov.sa" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
